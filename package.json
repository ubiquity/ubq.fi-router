{
  "name": "ubq-fi-router",
  "version": "1.0.0",
  "description": "UBQ.FI Router — Cloudflare Worker router to Deno apps",
  "main": "src/worker.ts",
  "scripts": {
    "cf:dev": "wrangler dev",
    "cf:deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.20.3"
  }
}
